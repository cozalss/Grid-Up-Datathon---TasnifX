# Grid Up Datathon - bagimliliklar
#
# SURUM POLITIKASI (onemli):
# Bu makinede pandas 3.0.3 / numpy 2.4.6 kurulu. Kaggle notebook imaji genellikle
# DAHA ESKI bir pandas (2.x) tasir. Ayni kod her ikisinde de calismali, bu yuzden:
#   - alt sinirlar gevsek, ust sinirlar YOK  (Kaggle'da pip install calistirilmaz)
#   - kodda pandas 3.0'da KALDIRILAN API'ler kullanilmaz (bkz. src/gridup/compat.py)
# Kurulum: powershell -ExecutionPolicy Bypass -File setup.ps1

# --- Cekirdek ---
pandas>=2.1
numpy>=1.26
scipy>=1.11
pyarrow>=14.0          # parquet: CSV'den 5-20x hizli okuma

# --- Modelleme ---
scikit-learn>=1.3
lightgbm>=4.0
xgboost>=2.0
catboost>=1.2

# --- Hiperparametre / aciklanabilirlik ---
optuna>=3.4
shap>=0.44

# --- Gorsellestirme ---
matplotlib>=3.8
seaborn>=0.13
plotly>=5.18

# --- Takvim / zaman serisi ---
holidays>=0.40         # TR resmi tatilleri (dini bayramlar hicri takvimle kayar)
statsmodels>=0.14      # STL ayristirma, ACF/PACF, durağanlık testleri

# --- Cografi (il/ilce eslestirme) ---
# geopandas agir bir bagimlilik; yalnizca gercekten poligon islemi gerekirse ac.
# geopandas>=0.14

# --- Harici veri cekme ---
requests>=2.31

# --- Notebook ---
jupyterlab>=4.0
ipywidgets>=8.1
nbstripout>=0.6        # notebook ciktilarini commit'ten once temizler

# --- Gelistirme ---
pytest>=7.4
pytest-cov>=4.1
hypothesis>=6.90
ruff>=0.5
mypy>=2.3
uv>=0.12
